# 新しい資料の作り方（オーサリングガイド）

このリポジトリの教材は **Markdown だけ書けば作れます**。HTML・QRコード・もくじ（index）は
`build.py` が自動生成するので、`docs/` の中を直接さわる必要はありません。

**1つの章 = 1つのフォルダ**です。フォルダの中に本文（index.md）と画像（image/index/）を入れます。
画像は VS Code で貼り付ければ自動で `image/index/` に保存されるので、フォルダは意識しなくてOKです。

```
content/                    ← ここだけ編集する
  _template/index.md        ← 新しい章のひな形
  b1_intro/                 ← 章フォルダ（フォルダ名 = <id>_<slug>）
    index.md                ←   本文（Markdown）
    image/index/            ←   画面キャプチャ（VS Codeが自動でここに保存）
templates/                  ← HTMLのひな形（基本さわらない）
build.py                    ← 変換スクリプト
docs/                       ← 自動生成される公開ファイル（さわらない）
  b1_intro/{index.html, qr.svg, image/index/}   ← content と同じ形で出力される
  assets/{deck.css, deck.js, hakase.png, qr.svg}
  index.html
```

章フォルダ名の頭は **b=基礎編 / e=拡張編 / mq=ロボットカー編 / c=コーディング編** ＋ 連番（例：`b6_temp`, `e2_servo`, `mq4_remote`, `c1_javascript`）。

---

## 手順（4ステップ）

1. **ひな形フォルダをコピー**して新しい章を作る
   ```bash
   cp -r content/_template content/e2_温度センサー
   ```
2. `content/e2_温度センサー/index.md` を**Markdownで書く**（下の「書き方」参照）
3. 画面キャプチャを本文に貼り付ける（VS Codeなら自動で `content/e2_温度センサー/image/index/` に保存。必要なら矢印などを付ける → 「マーキング」参照）
4. **ビルドして確認 → 公開**
   ```bash
   python build.py                 # docs/ を再生成（HTML・QR・もくじ）
   # docs/e2_温度センサー/index.html をブラウザで開いて確認
   git add -A && git commit -m "Add chapter e2" && git push
   ```
   push すると数十秒で GitHub Pages に反映されます。

> はじめて使うときだけ: `pip install segno pillow`（QR生成と画像縮小に使います）

---

## 書き方

### フロントマター（ファイル先頭の設定）

| 項目 | 説明 | 例 |
|---|---|---|
| `id` | 章の番号。ファイル名・QR・画像フォルダに使う | `06` / `mq4` |
| `slug` | URLになる英語名 | `thermo` |
| `section` | もくじのどのセクションに入れるか | `基礎編` / `拡張編` / `ロボットカー編` / `コーディング編` |
| `emoji` | もくじカードの絵文字 | `🌡️` |
| `num` | カードの番号バッジ | `6` / `④` |
| `color` | カードの枠色 | 基礎/拡張は `c1`〜`c5`、ロボットは `r1`〜`r3`、コーディングは `j1`〜`j3` |
| `nav_title` | 画面上部・タブに出るタイトル | `⑥ 温度センサーを使おう` |
| `card_title` | もくじカードの見出し | `温度センサーを使おう` |
| `desc` | もくじカードの説明（1〜2文） | `気温をはかって…` |
| `level` | （ロボットカー編だけ）難易度ラベル | `入門編` |
| `wip` | もくじカードに「🚧 工事中」マークを付ける（作りかけの章） | `true` / `準備中` |

### スライドの記法（本文）

| 書き方 | 意味 |
|---|---|
| `---`（行頭・単独） | スライドの区切り |
| `{cover}` | 表紙レイアウト（中央寄せ）。QRは自動で右下に付きます |
| `# 見出し` | スライドのタイトル |
| `:::` | ここで左右2カラムに分ける（左=説明、右=画像 が基本） |
| `1. ` / `- ` | 番号つき手順／箇条書き |
| `**太字**` `` `コード` `` | 強調・コード表示 |
| `![キャプション](image/index/xxx.png)` | 画像（連続で書くと横並び） |
| `> ふつうの文` | 博士のヒント吹き出し（緑） |
| `> ⚠ 文` | 注意の吹き出し（オレンジ） |
| `> 💪 文` | チャレンジの吹き出し（青） |
| ` ```js ` 〜 ` ``` ` | コードブロック（色分け・行番号・コピーボタン付き。下の「コードの見せ方」参照） |

**定番の並び**は「表紙 →『なにをするの？』→ 手順①②… 」です。`_template/index.md` がその形になっています。

### スマホで見たときの表示

パソコンでは 16:9 のスライドですが、**スマホなど幅780px以下の画面では自動で縦長のカードに変わります**。
書き方は同じままで、次のように組み替わります。

- `:::` の左右2カラム → **上に説明・下に画像**の縦並び（画像は横幅いっぱいに大きく表示）
- 連続して書いた画像（横並び）→ こちらも**縦に1枚ずつ大きく**
- 文字サイズは端末で読めるサイズに調整されます

**発表モード**と**印刷 / PDF** はスマホでも 16:9 のスライドのままです（スマホを縦に持ったまま発表モードにすると、画面を横向きに使うよう自動で回転します）。

### コードの見せ方（コーディング編）

JavaScript や Python のプログラムは、**コードブロック**で書きます。

````markdown
```js ハートを表示する          ← 「言語」＋（省略可）「キャプション」
basic.forever(function () {
    basic.showIcon(IconNames.Heart)
})
```
````

- 言語は `js`（＝JavaScript）と `py`（＝Python）。`javascript` / `python` と書いてもOK
- 言語のうしろに書いた文字は、コードの上に**小見出し**として出ます
- 色分け・**行番号**は自動です

表示は4つのモードで自動的に切りかわります。

| モード | 見え方 |
|---|---|
| **Web（パソコン）** | 右上の「📋 コピー」を押すと**ワンクリックでクリップボードにコピー**（行番号は入りません）。押すと「✅ コピーしました」に変わります |
| **スマホ** | 文字を読めるサイズ（13px）に拡大。長い行は**折り返して**表示。コピーボタンは指で押せる大きさになります |
| **発表モード** | 行数に合わせて文字が自動で大きくなります。コピーボタンは**消えます**（クリックで次のスライドに進むため） |
| **印刷 / PDF** | 背景と色分けはそのまま印刷。コピーボタンは**消えます** |

書くときのコツ：

- **1スライドのコードは 14行くらいまで**。長いと自動で文字が小さくなります（分けるのがおすすめ）
- メイクコードが作るコードに合わせて、**字下げは半角スペース4つ・行末のセミコロンなし**にすると、貼りつけたときに同じ見た目になります
- `:::` の右カラムをコードだけにすると、コード側が少し広く表示されます

---

## 画面キャプチャの渡し方

- いちばん簡単なのは **VS Code で画像をコピー → 本文に貼り付け**。自動で **`image/index/`** に保存され、`![](image/index/…)` の参照も入ります。**パスやフォルダは気にしなくてOK**（このリポジトリの `.vscode/settings.json` で保存先を `image/index/` に設定済み）
- 手で置くときも、その章の **`image/index/`** に入れて `![説明](image/index/step1.png)` と書けばOK
- ビルド時に `docs/<章>/image/index/` へコピーされ、大きすぎる画像は自動で縮小されます
- 名前は順番がわかるように `01_open.png` `02_block.png` … と付けると迷いません
- 形式は画面キャプチャなら **PNG** がおすすめ

---

## マーキング（矢印・囲み・「ここをクリック」）

画像に矢印や囲みを付けたいときは、**Greenshot**（Windows・無料）が手軽でおすすめです。

### Greenshot のセットアップ
1. https://getgreenshot.org/ からダウンロードしてインストール
2. `PrintScreen` キーで範囲を選んでキャプチャ → そのまま注釈エディタが開く
3. ツールバーで **矢印・四角・ハイライト・番号・文字** を追加
4. 「名前を付けて保存」で `content/<章>/image/index/` に PNG 保存（または VS Code に貼り付け）

> **ShareX**（https://getshar.com/ ）も同様に無料で高機能です。お好みで。

### マーキングのコツ（子ども向け）
- 押す場所は **赤い四角で囲む** ＋ 短い言葉（「①ここをクリック」）
- 1枚に矢印を入れすぎない（多くても2〜3個）
- そもそも本文の「**番号つき手順**」＋スクショで大半は伝わるので、マーキングは"ここぞ"だけに

---

## もくじのセクションについて

`section` に書いた名前でカードが自動的に振り分けられます。

- `基礎編` … micro:bit だけ（外部デバイスなし）
- `拡張編` … 外部デバイスをつなぐ
- `ロボットカー編` … micro:Maqueen（`level` も付ける）
- `コーディング編` … JavaScript / Python の文字のプログラム

順番は `id` の昇順です。新しいセクションを増やしたいときは `build.py` の `SECTIONS` に追記してください。
